import { useState } from "react";
import {
  Box,
  Card,
  CardContent,
  Grid,
  Typography,
  Chip,
  Divider,
  Paper,
} from "@mui/material";

import AccountTreeIcon from "@mui/icons-material/AccountTree";

import { RichTreeView } from "@mui/x-tree-view/RichTreeView";

const treeItems = [
  {
    id: "root",
    label: "Person A",
    type: "root",
    children: [
      {
        id: "asc",
        label: "Analog Signal Chain",
        type: "group",
        children: [
          {
            id: "savitri",
            label: "Savitri Sitaraman",
            type: "person",
          },
          {
            id: "sandeep",
            label: "Sandeep Oswal",
            type: "person",
          },
        ],
      },
      {
        id: "power",
        label: "Analog Power",
        type: "group",
        children: [
          {
            id: "manager1",
            label: "Manager A",
            type: "person",
          },
        ],
      },
      {
        id: "ep",
        label: "Embedded Processors",
        type: "group",
      },
      {
        id: "tmg",
        label: "TMG Tech Group",
        type: "group",
      },
    ],
  },
];

const detailsData = {
  savitri: {
    name: "Savitri Sitaraman",
    role: "Manager",
    managedUnits: ["ASC High Speed Data"],
    slas: ["SLA-1", "SLA-3"],
    totalFlows: 124,
  },

  sandeep: {
    name: "Sandeep Oswal",
    role: "Manager",
    managedUnits: ["ASC Audio", "PADC", "DAC"],
    slas: ["SLA-2", "SLA-5", "SLA-7"],
    totalFlows: 342,
  },

  asc: {
    name: "Analog Signal Chain",
    role: "Organization",
    managers: ["Savitri", "Sandeep"],
    totalUnits: 4,
  },

  power: {
    name: "Analog Power",
    role: "Organization",
    managers: ["Manager A"],
    totalUnits: 2,
  },
};

export default function App() {
  const [selectedNode, setSelectedNode] = useState("asc");

  const details = detailsData[selectedNode];

  return (
    <Box
      sx={{
        height: "100vh",
        bgcolor: "#f5f7fa",
        p: 2,
      }}
    >
      <Typography
        variant="h4"
        fontWeight="bold"
        mb={2}
      >
        SLA Ownership Dashboard
      </Typography>

      <Grid container spacing={2}>
        {/* LEFT PANEL */}
        <Grid item xs={3}>
          <Paper
            sx={{
              height: "85vh",
              p: 2,
            }}
          >
            <Typography
              variant="h6"
              gutterBottom
            >
              Organization Tree
            </Typography>

            <Divider sx={{ mb: 2 }} />

            <RichTreeView
              items={treeItems}
              onItemClick={(event, itemId) =>
                setSelectedNode(itemId)
              }
            />
          </Paper>
        </Grid>

        {/* CENTER PANEL */}
        <Grid item xs={5}>
          <Paper
            sx={{
              height: "85vh",
              p: 2,
            }}
          >
            <Typography
              variant="h6"
              gutterBottom
            >
              Details
            </Typography>

            <Divider sx={{ mb: 2 }} />

            {!details ? (
              <Typography>
                Select a node from the tree
              </Typography>
            ) : (
              <>
                <Typography
                  variant="h5"
                  fontWeight="bold"
                >
                  {details.name}
                </Typography>

                <Typography
                  color="text.secondary"
                  mb={3}
                >
                  {details.role}
                </Typography>

                {details.managedUnits && (
                  <>
                    <Typography
                      variant="subtitle1"
                      fontWeight="bold"
                    >
                      Managed Units
                    </Typography>

                    <Box mt={1}>
                      {details.managedUnits.map((unit) => (
                        <Chip
                          key={unit}
                          label={unit}
                          sx={{ mr: 1, mb: 1 }}
                        />
                      ))}
                    </Box>
                  </>
                )}

                {details.managers && (
                  <>
                    <Typography
                      variant="subtitle1"
                      fontWeight="bold"
                      mt={2}
                    >
                      Managers
                    </Typography>

                    <Box mt={1}>
                      {details.managers.map((manager) => (
                        <Chip
                          key={manager}
                          label={manager}
                          color="primary"
                          sx={{ mr: 1, mb: 1 }}
                        />
                      ))}
                    </Box>
                  </>
                )}
              </>
            )}
          </Paper>
        </Grid>

        {/* RIGHT PANEL */}
        <Grid item xs={4}>
          <Paper
            sx={{
              height: "85vh",
              p: 2,
            }}
          >
            <Typography
              variant="h6"
              gutterBottom
            >
              SLA Overview
            </Typography>

            <Divider sx={{ mb: 2 }} />

            {details?.slas ? (
              <>
                <Card sx={{ mb: 2 }}>
                  <CardContent>
                    <Typography variant="h4">
                      {details.totalFlows}
                    </Typography>

                    <Typography color="text.secondary">
                      Total Entered Flows
                    </Typography>
                  </CardContent>
                </Card>

                <Typography
                  variant="subtitle1"
                  fontWeight="bold"
                  mb={1}
                >
                  SLA Mapping
                </Typography>

                {details.slas.map((sla) => (
                  <Card
                    key={sla}
                    sx={{
                      mb: 1,
                    }}
                  >
                    <CardContent>
                      <Typography>
                        {sla}
                      </Typography>
                    </CardContent>
                  </Card>
                ))}
              </>
            ) : (
              <Typography color="text.secondary">
                Select a person to view SLA details
              </Typography>
            )}
          </Paper>
        </Grid>
      </Grid>
    </Box>
  );
}

npm install reactflow
npm install @mui/material @emotion/react @emotion/styled

import { useState } from "react";
import ReactFlow, {
  Background,
  Controls,
  MiniMap,
} from "reactflow";

import "reactflow/dist/style.css";

import {
  Box,
  Drawer,
  Typography,
  Chip,
  Divider,
} from "@mui/material";

const nodeDetails = {
  root: {
    title: "Person A",
    type: "Root Owner",
  },

  asc: {
    title: "Analog Signal Chain",
    type: "Organization",
    managers: 2,
  },

  savitri: {
    title: "Savitri Sitaraman",
    type: "Manager",
    units: ["ASC High Speed Data"],
    slas: ["SLA-1", "SLA-3"],
    flows: 124,
  },

  sandeep: {
    title: "Sandeep Oswal",
    type: "Manager",
    units: ["ASC Audio", "PADC", "DAC"],
    slas: ["SLA-2", "SLA-5", "SLA-7"],
    flows: 342,
  },

  hsd: {
    title: "ASC High Speed Data",
    type: "SB1",
  },
};

const nodes = [
  {
    id: "root",
    position: { x: 500, y: 0 },
    data: { label: "Person A" },
  },

  {
    id: "asc",
    position: { x: 450, y: 120 },
    data: { label: "Analog Signal Chain" },
  },

  {
    id: "power",
    position: { x: 850, y: 120 },
    data: { label: "Analog Power" },
  },

  {
    id: "savitri",
    position: { x: 350, y: 260 },
    data: { label: "Savitri Sitaraman" },
  },

  {
    id: "sandeep",
    position: { x: 650, y: 260 },
    data: { label: "Sandeep Oswal" },
  },

  {
    id: "hsd",
    position: { x: 350, y: 420 },
    data: { label: "ASC High Speed Data" },
  },

  {
    id: "audio",
    position: { x: 650, y: 420 },
    data: { label: "ASC Audio" },
  },

  {
    id: "padc",
    position: { x: 520, y: 520 },
    data: { label: "PADC" },
  },

  {
    id: "dac",
    position: { x: 780, y: 520 },
    data: { label: "DAC" },
  },
];

const edges = [
  {
    id: "e1",
    source: "root",
    target: "asc",
  },

  {
    id: "e2",
    source: "root",
    target: "power",
  },

  {
    id: "e3",
    source: "asc",
    target: "savitri",
  },

  {
    id: "e4",
    source: "asc",
    target: "sandeep",
  },

  {
    id: "e5",
    source: "savitri",
    target: "hsd",
  },

  {
    id: "e6",
    source: "sandeep",
    target: "audio",
  },

  {
    id: "e7",
    source: "sandeep",
    target: "padc",
  },

  {
    id: "e8",
    source: "sandeep",
    target: "dac",
  },
];

export default function App() {
  const [selected, setSelected] = useState(nodeDetails.root);

  const onNodeClick = (_, node) => {
    if (nodeDetails[node.id]) {
      setSelected(nodeDetails[node.id]);
    }
  };

  return (
    <Box
      sx={{
        height: "100vh",
        width: "100%",
      }}
    >
      <ReactFlow
        nodes={nodes}
        edges={edges}
        fitView
        onNodeClick={onNodeClick}
      >
        <Background />
        <MiniMap />
        <Controls />
      </ReactFlow>

      <Drawer
        variant="permanent"
        anchor="right"
        sx={{
          width: 350,
          flexShrink: 0,
          "& .MuiDrawer-paper": {
            width: 350,
            p: 3,
          },
        }}
      >
        <Typography variant="h5">
          {selected.title}
        </Typography>

        <Typography
          color="text.secondary"
          mb={2}
        >
          {selected.type}
        </Typography>

        <Divider sx={{ mb: 2 }} />

        {selected.managers && (
          <>
            <Typography>
              Managers: {selected.managers}
            </Typography>
          </>
        )}

        {selected.units && (
          <>
            <Typography
              variant="h6"
              mt={2}
            >
              Managed Units
            </Typography>

            {selected.units.map((unit) => (
              <Chip
                key={unit}
                label={unit}
                sx={{ mr: 1, mt: 1 }}
              />
            ))}
          </>
        )}

        {selected.slas && (
          <>
            <Typography
              variant="h6"
              mt={3}
            >
              SLA Mapping
            </Typography>

            {selected.slas.map((sla) => (
              <Chip
                key={sla}
                color="primary"
                label={sla}
                sx={{ mr: 1, mt: 1 }}
              />
            ))}
          </>
        )}

        {selected.flows && (
          <>
            <Typography
              variant="h6"
              mt={3}
            >
              Total Flows
            </Typography>

            <Typography variant="h4">
              {selected.flows}
            </Typography>
          </>
        )}
      </Drawer>
    </Box>
  );
}
